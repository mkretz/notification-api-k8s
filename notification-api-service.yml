apiVersion: v1
kind: Service
metadata:
  name: notification-api
  labels:
    name: notification-api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    name: notification-api
